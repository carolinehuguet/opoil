<div class="container">
  <h1>Les chiens près de chez vous</h1>

  <div class="cards">
  <% @dogs.each do |dog| %>
    <div class="card-category">
      <h5><%= link_to dog.name, dog_path %></h5>
    </div>
  <% end %>
  </div>

</div>
